{
  "name": "notes-to-blog",
  "version": "1.0.0",
  "description": "```bash osascript blog.js > blog.json ```",
  "main": "blog.js",
  "type": "module",
  "scripts": {
    "build": "bun build ./process-notes.js --compile --outfile ./bin/generate"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cheerio": "^1.0.0"
  }
}
